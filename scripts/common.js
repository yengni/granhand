/* full swiper 설정 */
const mainS = new Swiper(".mainPage", {
    direction : 'vertical',
    mousewheel : true
})/* 
const bestS = new Swiper('.best_item', {
    autoplay : {delay:1000},
    loop : true
}) */